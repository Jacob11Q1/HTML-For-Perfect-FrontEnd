# HTML For Perfect Frontend 🧱✨

A structured set of **HTML-only challenges** designed to build _real_ frontend foundations:
semantic HTML, accessibility, forms, tables, and clean document structure — step by step.

> Goal: write HTML like a professional before touching heavy CSS/JS frameworks.

---

## What’s inside

### ✅ Challenge 1 — Perfect Page Skeleton

- Proper HTML5 boilerplate
- Semantic layout: `header`, `nav`, `main`, `section`, `footer`
- Correct heading hierarchy (single `h1`)
- Anchor navigation using `id` targets

### ✅ Challenge 2 — Lists, Tables & Images

- `ul/li` for structured content
- Accessible tables: `caption`, `thead`, `tbody`, `scope`
- Images with meaningful `alt` text
- Basic performance: `loading="lazy"`

### ✅ Challenge 3 — Real-World Forms

- Labels done correctly (`for` + `id`)
- Required fields + validation basics
- `fieldset` + `legend` for grouping
- Radios, select dropdowns, checkbox agreements

### ✅ Challenge 4 — Accessibility & SEO Polish

- Skip link for keyboard users
- Meta description for SEO
- Better landmark usage
- Semantic contact info with `<address>`
- `aria-describedby` hints for forms

---

## Project structure

```txt
HTML-For-Perfect-FrontEnd/
├─ index.html
├─ style.css              # optional (HTML-first focus)
└─ assets/
   └─ coffee-cup.jpg
```
